let bonus = 20;

function sum(first, second) {
    let result = first + second + bonus;
    //   console.log(bonus);
    if (bonus > 10) {
        // let mood = "happy";
        var mood = "fishy";
        mood = "dummy";
        // console.log(mood);
    }
    console.log(day);
    var day = "friday";
    return result;
}

const output = sum(3, 10);
// console.log(bonus);
// console.log(output);